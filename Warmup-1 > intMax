Warmup-1 > intMax 

Given three int values, a b c, return the largest.

intMax(1, 2, 3) → 3
intMax(1, 3, 2) → 3
intMax(3, 2, 1) → 3

--------------------------------------------

public int intMax(int a, int b, int c) {
  if (a > Math.max(b,c)) return a;
  if (b > Math.max(a,c)) return b;
  return c;
}
